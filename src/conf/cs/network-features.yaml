defaults:
  - models/mlr
  - models/rf
  - models/svr
  - models/mlp
  - _self_
experiments:
  COUPLED-C:
    files:
      data: C-feature_1_metric_15-03092023.csv
    paths:
      log: ${hydra:runtime.cwd}/logs/ARCADE/
      data: ${hydra:runtime.cwd}/data/ARCADE/
      results: ${hydra:runtime.cwd}/results/ARCADE/
  # COUPLED-CHX:
  #   files:
  #     data: CH-feature_1_metric_15-03092023.csv
  #   paths:
  #     log: ${hydra:runtime.cwd}/logs/ARCADE/
  #     data: ${hydra:runtime.cwd}/data/ARCADE/
  #     results: ${hydra:runtime.cwd}/results/ARCADE/
data:
  features:
    - NODES
    - EDGES
    - RADIUS
    - DIAMETER
    - AVG_ECCENTRICITY
    - AVG_SHORTEST_PATH
    - AVG_IN_DEGREES
    - AVG_OUT_DEGREES
    - AVG_DEGREE
    - AVG_CLUSTERING
    - AVG_CLOSENESS
    - AVG_BETWEENNESS
    # - RADIUS_FLOW
    # - DIAMETER_FLOW
    # - AVG_ECCENTRICITY_FLOW
    # - AVG_SHORTEST_PATH_FLOW
    - RADIUS_OXYGEN_AVG
    - DIAMETER_OXYGEN_AVG
    - AVG_ECCENTRICITY_OXYGEN_AVG
    - AVG_SHORTEST_PATH_OXYGEN_AVG
    - RADIUS_OXYGEN_DELTA
    - DIAMETER_OXYGEN_DELTA
    - AVG_ECCENTRICITY_OXYGEN_DELTA
    - AVG_SHORTEST_PATH_OXYGEN_DELTA
    - RADIUS_PRESSURE_AVG
    - DIAMETER_PRESSURE_AVG
    - AVG_ECCENTRICITY_PRESSURE_AVG
    - AVG_SHORTEST_PATH_PRESSURE_AVG
    - RADIUS_PRESSURE_DELTA
    - DIAMETER_PRESSURE_DELTA
    - AVG_ECCENTRICITY_PRESSURE_DELTA
    - AVG_SHORTEST_PATH_PRESSURE_DELTA
    - RADIUS_RADIUS
    - DIAMETER_RADIUS
    - AVG_ECCENTRICITY_RADIUS
    - AVG_SHORTEST_PATH_RADIUS
    - RADIUS_SHEAR
    - DIAMETER_SHEAR
    - AVG_ECCENTRICITY_SHEAR
    - AVG_SHORTEST_PATH_SHEAR
    - RADIUS_WALL
    - DIAMETER_WALL
    - AVG_ECCENTRICITY_WALL
    - AVG_SHORTEST_PATH_WALL
    - RADIUS
    - LENGTH
    - WALL
    - SHEAR
    - CIRCUM
    - FLOW
  response:
    - ACTIVITY
    - GROWTH
    - CYCLES
    - SYMMETRY
    # - GLUCOSE
    # - OXYGEN
