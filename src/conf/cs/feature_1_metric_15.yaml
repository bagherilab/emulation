defaults:
  - models/mlr
  - models/rf
  - models/svr
  - models/mlp
  - features/naive
  - _self_
experiments:
  COUPLED-C:
    files:
      data: VASCULAR_FUNCTION_C.csv
    paths:
      log: ${hydra:runtime.cwd}/logs/ARCADE/
      data: ${hydra:runtime.cwd}/data/ARCADE/
      results: ${hydra:runtime.cwd}/results/ARCADE/
  COUPLED-CHX:
    files:
      data: VASCULAR_FUNCTION_CHX.csv
    paths:
      log: ${hydra:runtime.cwd}/logs/ARCADE/
      data: ${hydra:runtime.cwd}/data/ARCADE/
      results: ${hydra:runtime.cwd}/results/ARCADE/
response:
  - ACTIVITY
  - GROWTH
  - CYCLES
  - SYMMETRY
  - GLUCOSE
  - OXYGEN
